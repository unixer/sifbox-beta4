#!/bin/sh

. config/options
mkdir -p $BUILD/picon-1.0
#mkdir -p ${BUILD}-${PKG_VERSION}
#mkdir -p $BUILD/'$1'-'$PKG_VERSION'
cd $BUILD/$1*

tar jxf $ROOT/$PACKAGES/$1/picon/picon.tar.bz2
#do_configure
#make
#make_install
